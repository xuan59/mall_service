<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.entity.BrowsingHistoryDao">

    <resultMap type="com.mall.entity.BrowsingHistory" id="BrowsingHistoryMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="browsingName" column="browsing_name" jdbcType="VARCHAR"/>
        <result property="browsingNumber" column="browsing_number" jdbcType="OTHER"/>
        <result property="browsingDate" column="browsing_date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BrowsingHistoryMap">
        select
          id, user_name, browsing_name, browsing_number, browsing_date
        from mall.browsing_history
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BrowsingHistoryMap">
        select
          id, user_name, browsing_name, browsing_number, browsing_date
        from mall.browsing_history
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BrowsingHistoryMap">
        select
        id, user_name, browsing_name, browsing_number, browsing_date
        from mall.browsing_history
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userName != null and userName != ''">
                and user_name = #{userName}
            </if>
            <if test="browsingName != null and browsingName != ''">
                and browsing_name = #{browsingName}
            </if>
            <if test="browsingNumber != null">
                and browsing_number = #{browsingNumber}
            </if>
            <if test="browsingDate != null">
                and browsing_date = #{browsingDate}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into mall.browsing_history(user_name, browsing_name, browsing_number, browsing_date)
        values (#{userName}, #{browsingName}, #{browsingNumber}, #{browsingDate})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into mall.browsing_history(user_name, browsing_name, browsing_number, browsing_date)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userName}, #{entity.browsingName}, #{entity.browsingNumber}, #{entity.browsingDate})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into mall.browsing_history(user_name, browsing_name, browsing_number, browsing_date)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userName}, #{entity.browsingName}, #{entity.browsingNumber}, #{entity.browsingDate})
        </foreach>
        on duplicate key update
        user_name = values(user_name) , browsing_name = values(browsing_name) , browsing_number =
        values(browsing_number) , browsing_date = values(browsing_date)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update mall.browsing_history
        <set>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="browsingName != null and browsingName != ''">
                browsing_name = #{browsingName},
            </if>
            <if test="browsingNumber != null">
                browsing_number = #{browsingNumber},
            </if>
            <if test="browsingDate != null">
                browsing_date = #{browsingDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from mall.browsing_history where id = #{id}
    </delete>

</mapper>